<nav class="navbar navbar-fixed-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
    <% if user_signed_in? %>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <%= image_tag current_user.profile.avatar.url(:thumb).to_s, :class => "img-circle avatar" %>
      </button>
    <% else %>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-navbar-collapse">
        <%= link_to new_user_session_path, :class => 'navbar-link' do %>
          <span class="glyphicon glyphicon-log-in" style="font-size: 16px;"></span> <span class="sign-in-text">Sign In</span>
        <% end %>
      </button>
    <% end %>
      <span class="navbar-brand">
        <a href="/">investomni</a>
      </span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="create-recommendation">
          <%= button_to new_recommendation_path, :class => "btn btn-primary btn-sm", :method => "get" do %>
            <span class="glyphicon glyphicon-plus"></span> <span class="btn-text">Create</span>
          <% end %>
        </li>
        <li class="dropdown">
        <% if user_signed_in? %>
          <%= link_to image_tag(current_user.profile.avatar.url(:thumb).to_s, :class => "img-circle avatar"), current_user.profile %>
          <ul class="dropdown-menu">
            <li>
              <%= link_to current_user.profile do %>
                <span class="glyphicon glyphicon-user"></span> My Profile
              <% end %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete, :class => "gray" do %>
                <span class="glyphicon glyphicon-off"></span> Sign Out
              <% end %>
            </li>
          </ul>
        <% else %>
        <%= link_to new_user_session_path, :class => 'navbar-link' do %>
          <span class="glyphicon glyphicon-log-in"></span> Sign In
        <% end %>
        <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->

    <!-- collapse dropdown menu -->
    <% if user_signed_in? %>
    <div class="collapse" id="bs-navbar-collapse">
      <ul class="nav">
        <li>
          <%= link_to new_recommendation_path do %>
            <span class="glyphicon glyphicon-plus"></span> Create
          <% end %>
        </li>
        <li>
          <%= link_to current_user.profile do %>
            <span class="glyphicon glyphicon-user"></span> My Profile
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, method: :delete, :class => "gray" do %>
            <span class="glyphicon glyphicon-off"></span> Sign Out
          <% end %>
        </li>
      </ul>
    <div>
    <% end %>

  </div><!-- /.container-fluid -->
</nav>