<%= form_for(@trade) do |f| %>
  <% if @trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trade.errors.count, "error") %> prohibited this trade from being saved:</h2>

      <ul>
      <% @trade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :symbol %><br>
    <%= f.text_field :symbol %>
  </div>
 
  <div>
    <%= f.label :action %><br>
    <%= f.select :action, [ :buy, :sell ], :prompt => 'Select One' %>
  </div>

  <div>
    <%= f.label :shares %><br>
    <%= f.text_field :shares %>
  </div>

  <div>
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
